# Frontend Rules Skill
<!-- Trigger: Invoke when reading/editing files under /frontend/** -->

## Quick Reference
- Components: `frontend/src/components/`
- State: `frontend/src/store/`
- Styling: `frontend/src/styles/`

## Key Constraints
- **NEVER** use inline styles; use Tailwind/CSS modules
- **NEVER** dump full component trees; read targeted files only
- All new components MUST include unit tests

## Full Rules
See: `.claude/rules/frontend.md`
